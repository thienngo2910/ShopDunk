@model IEnumerable<ShopDunk.Models.Product>
@{
 ViewBag.Title = "Qu?n lý s?n ph?m";
}

<h2>Qu?n lý s?n ph?m</h2>

<p>
 <a class="btn btn-success" href="@Url.Action("Create", "Product")">Thêm s?n ph?m m?i</a>
 <a class="btn btn-secondary" href="@Url.Action("Index", "Admin")">Tr? v?</a>
</p>

@if (TempData["Success"] != null)
{
 <div class="alert alert-success">@TempData["Success"]</div>
}

<table class="table">
 <thead>
 <tr>
 <th>Tên</th>
 <th>Giá</th>
 <th>Kho</th>
 <th>Danh m?c</th>
 <th>Hành ??ng</th>
 </tr>
 </thead>
 <tbody>
 @foreach (var p in Model)
 {
 <tr>
 <td>@p.Name</td>
 <td>@p.Price.ToString("C")</td>
 <td>@p.Stock</td>
 <td>@p.Category</td>
 <td>
 <a class="btn btn-sm btn-primary" href="@Url.Action("Edit", "Product", new { id = p.ProductID })">S?a</a>
 <a class="btn btn-sm btn-danger" href="@Url.Action("Delete", "Product", new { id = p.ProductID })">Xóa</a>
 </td>
 </tr>
 }
 </tbody>
</table>
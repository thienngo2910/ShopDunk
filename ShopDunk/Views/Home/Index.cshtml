@model IEnumerable<ShopDunk.Models.Product>
@{
    ViewBag.Title = "Trang chủ";
}

<div class="text-center mb-5">
    <h1 class="display-5 fw-bold">Chào mừng đến với ShopDunk</h1>
    <p class="lead">Khám phá các sản phẩm Apple chính hãng</p>
</div>
@{
    ViewBag.Title = "Trang chủ";
}

<!-- Slider -->
<div id="shopdunkSlider" class="carousel slide mb-5" data-bs-ride="carousel" data-bs-interval="3000">
    <div class="carousel-inner">
        <div class="carousel-item active">
            <img src="@Url.Content("~/Images/Banners/banner1.png")" class="d-block w-100" alt="Banner 1">
        </div>
        <div class="carousel-item">
            <img src="@Url.Content("~/Images/Banners/banner2.png")" class="d-block w-100" alt="Banner 2">
        </div>
        <div class="carousel-item">
            <img src="@Url.Content("~/Images/Banners/banner3.png")" class="d-block w-100" alt="Banner 3">
        </div>
    </div>

    <!-- Nút chuyển ảnh -->
    <button class="carousel-control-prev" type="button" data-bs-target="#shopdunkSlider" data-bs-slide="prev">
        <span class="carousel-control-prev-icon"></span>
        <span class="visually-hidden">Trước</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#shopdunkSlider" data-bs-slide="next">
        <span class="carousel-control-next-icon"></span>
        <span class="visually-hidden">Tiếp</span>
    </button>

    <!-- Nút chọn ảnh -->
    <div class="carousel-indicators">
        <button type="button" data-bs-target="#shopdunkSlider" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
        <button type="button" data-bs-target="#shopdunkSlider" data-bs-slide-to="1" aria-label="Slide 2"></button>
        <button type="button" data-bs-target="#shopdunkSlider" data-bs-slide-to="2" aria-label="Slide 3"></button>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<!-- iPhone -->
<h2 class="mb-4">iPhone</h2>
<div class="row">
    @foreach (var item in ViewBag.iPhones as List<ShopDunk.Models.Product>)
    {
        <div class="col-md-3 mb-4">
            <div class="card border-0 shadow-sm">
                <img src="@item.ImageUrl" class="card-img-top" alt="@item.Name">
                <div class="card-body text-center">
                    <h5 class="card-title">@item.Name</h5>
                    <p class="text-danger fw-bold">@item.Price.ToString("N0") ₫</p>
                    <a href="/Cart/Add/@item.ProductID" class="btn btn-primary w-100">Thêm vào giỏ</a>
                </div>
            </div>
        </div>
    }
</div>

<!-- iPad -->
<h2 class="mb-4">iPad</h2>
<div class="row">
    @foreach (var item in ViewBag.iPads as List<ShopDunk.Models.Product>)
    {
        <div class="col-md-3 mb-4">
            <div class="card border-0 shadow-sm">
                <img src="@item.ImageUrl" class="card-img-top" alt="@item.Name">
                <div class="card-body text-center">
                    <h5 class="card-title">@item.Name</h5>
                    <p class="text-danger fw-bold">@item.Price.ToString("N0") ₫</p>
                    <a href="/Cart/Add/@item.ProductID" class="btn btn-primary w-100">Thêm vào giỏ</a>
                </div>
            </div>
        </div>
    }
</div>

<!-- Mac -->
<h2 class="mb-4">Mac</h2>
<div class="row">
    @foreach (var item in ViewBag.Macs as List<ShopDunk.Models.Product>)
    {
        <div class="col-md-3 mb-4">
            <div class="card border-0 shadow-sm">
                <img src="@item.ImageUrl" class="card-img-top" alt="@item.Name">
                <div class="card-body text-center">
                    <h5 class="card-title">@item.Name</h5>
                    <p class="text-danger fw-bold">@item.Price.ToString("N0") ₫</p>
                    <a href="/Cart/Add/@item.ProductID" class="btn btn-primary w-100">Thêm vào giỏ</a>
                </div>
            </div>
        </div>
    }
</div>
